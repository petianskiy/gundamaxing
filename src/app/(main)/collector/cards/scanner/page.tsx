import { ScannerShell } from "./scanner-shell";

export const metadata = {
  title: "Card Scanner | Gundamaxing",
  description: "Scan your Gundam Card Game cards to add them to your collection.",
};

export default function ScannerPage() {
  return <ScannerShell />;
}
